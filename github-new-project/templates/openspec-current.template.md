# Current Work

## Resume Protocol

在新 AI 窗口中，先说：`继续 openspec/CURRENT.md`

然后先读取：

1. `git status --short --branch`
2. `git log --oneline -5`
3. `openspec/project.md`

只在任务需要时再深入相关 spec 或 change。

## Branch

`main`

## Current State

- 仓库目前是协作基线状态
- Git、GitHub、SSH、PR-Agent、OpenSpec 文档底座是当前重点
- 尚未引入具体技术栈和业务代码

## Next Step

1. 按真实需求选择技术栈
2. 通过新的 OpenSpec change 记录语言与工程化选择
3. 在基线之上逐步补齐构建、测试、发布流程
